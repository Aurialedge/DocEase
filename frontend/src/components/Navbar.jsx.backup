import React, { useState } from 'react'
import { assets } from '../assets/assets'
import { NavLink, useNavigate } from 'react-router-dom'
import ProfileCardModal from './ProfileCardModal'

const Navbar = () => {

  const navigate = useNavigate();

  const [showMenu, setShowMenu] = useState(false);
  const [token, setToken] = useState(true);//if we have token we will be logged in otherwise not
  const [showProfile, setShowProfile] = useState(false);
  const [showProfileCard, setShowProfileCard] = useState(false);

  // Mock user data - in a real app, this would come from context/API
  const userData = {
    name: "Shivraj Perkar",
    image: assets.profile_pic,
    email: 'saperkar1862004@gmail.com'
  };

  // Close dropdowns when menu opens
  const handleMenuToggle = () => {
    setShowMenu(!showMenu);
    setShowProfile(false); // Close profile dropdown when menu opens
  };

  // Close menu when profile dropdown opens
  const handleProfileToggle = () => {
    setShowProfile(!showProfile);
    setShowMenu(false); // Close menu when profile dropdown opens
  };

  return (
    <nav className='sticky top-0 z-50 bg-white/95 backdrop-blur-md border-b border-gray-200 shadow-sm'>
      <div className='max-w-7xl mx-auto px-4 sm:px-6 py-3 sm:py-4'>
        <div className='flex items-center justify-between'>
          
          {/* Logo */}
          <div 
            onClick={() => navigate('/home')} 
            className='flex items-center gap-2 sm:gap-3 cursor-pointer group'
          >
            <div className='w-10 h-10 sm:w-12 sm:h-12 bg-gradient-to-r from-primary to-primary/80 rounded-xl flex items-center justify-center shadow-lg group-hover:shadow-xl transition-all duration-300'>
              <img src={assets.logo} alt="logo" className="w-6 h-6 sm:w-8 sm:h-8" />
            </div>
            <div className='hidden sm:block'>
              <h1 className='text-lg sm:text-xl font-bold text-gray-900'>DocEase</h1>
              <p className='text-xs text-gray-500 hidden lg:block'>Healthcare Made Simple</p>
            </div>
          </div>

          {/* Desktop Navigation */}
          <div className='hidden lg:flex items-center gap-6 xl:gap-8'>
            <NavLink 
              to='/home' 
              className={({ isActive }) => `
                relative py-2 px-2 sm:px-3 text-sm font-medium transition-all duration-300
                ${isActive 
                  ? 'text-primary' 
                  : 'text-gray-600 hover:text-primary'
                }
              `}
            >
              {({ isActive }) => (
                <>
                  Home
                  {isActive && (
                    <div className='absolute bottom-0 left-0 right-0 h-0.5 bg-gradient-to-r from-primary to-primary/80 rounded-full animate-fade-in'></div>
                  )}
                </>
              )}
            </NavLink>
            
            <NavLink 
              to='/doctors' 
              className={({ isActive }) => `
                relative py-2 px-2 sm:px-3 text-sm font-medium transition-all duration-300
                ${isActive 
                  ? 'text-primary' 
                  : 'text-gray-600 hover:text-primary'
                }
              `}
            >
              {({ isActive }) => (
                <>
                  Doctors
                  {isActive && (
                    <div className='absolute bottom-0 left-0 right-0 h-0.5 bg-gradient-to-r from-primary to-primary/80 rounded-full animate-fade-in'></div>
                  )}
                </>
              )}
            </NavLink>
            
            <NavLink 
              to='/about' 
              className={({ isActive }) => `
                relative py-2 px-2 sm:px-3 text-sm font-medium transition-all duration-300
                ${isActive 
                  ? 'text-primary' 
                  : 'text-gray-600 hover:text-primary'
                }
              `}
            >
              {({ isActive }) => (
                <>
                  About
                  {isActive && (
                    <div className='absolute bottom-0 left-0 right-0 h-0.5 bg-gradient-to-r from-primary to-primary/80 rounded-full animate-fade-in'></div>
                  )}
                </>
              )}
            </NavLink>
            
            <NavLink 
              to='/contact' 
              className={({ isActive }) => `
                relative py-2 px-2 sm:px-3 text-sm font-medium transition-all duration-300
                ${isActive 
                  ? 'text-primary' 
                  : 'text-gray-600 hover:text-primary'
                }
              `}
            >
              {({ isActive }) => (
                <>
                  Contact
                  {isActive && (
                    <div className='absolute bottom-0 left-0 right-0 h-0.5 bg-gradient-to-r from-primary to-primary/80 rounded-full animate-fade-in'></div>
                  )}
                </>
              )}
            </NavLink>
          </div>

          {/* Right Side Actions */}
          <div className='flex items-center gap-2 sm:gap-4'>
            
            {/* Profile Section */}
            {token ? (
              <div className='relative'>
                <button
                  onClick={() => setShowProfileCard(true)}
                  className='flex items-center gap-2 sm:gap-3 p-2 rounded-xl hover:bg-gray-50 transition-colors duration-200 group'
                >
                  <div className='relative'>
                    <img 
                      className='w-8 h-8 sm:w-10 sm:h-10 rounded-full border-2 border-white shadow-md group-hover:shadow-lg transition-all duration-300' 
                      src={assets.profile_pic} 
                      alt="Profile" 
                    />
                    <div className='absolute -bottom-1 -right-1 w-3 h-3 sm:w-4 sm:h-4 bg-green-500 border-2 border-white rounded-full'></div>
                  </div>
                  <div className='hidden md:block text-left'>
                    <p className='text-xs sm:text-sm font-semibold text-gray-900'>John Doe</p>
                    <p className='text-xs text-gray-500 hidden lg:block'>Premium Member</p>
                  </div>
                  <svg 
                    className={`w-3 h-3 sm:w-4 sm:h-4 text-gray-400 transition-transform duration-300 ${showProfile ? 'rotate-180' : ''}`} 
                    fill="none" 
                    stroke="currentColor" 
                    viewBox="0 0 24 24"
                  >
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
                  </svg>
                </button>

                {/* Profile Dropdown */}
                <div className={`absolute right-0 mt-2 w-64 sm:w-72 bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden transition-all duration-300 ${
                  showProfile ? 'opacity-100 translate-y-0' : 'opacity-0 -translate-y-2 pointer-events-none'
                }`}>
                  <div className='p-4 border-b border-gray-100'>
                    <div className='flex items-center gap-3'>
                      <img 
                        className='w-12 h-12 rounded-full' 
                        src={assets.profile_pic} 
                        alt="Profile" 
                      />
                      <div>
                        <p className='font-semibold text-gray-900'>John Doe</p>
                        <p className='text-sm text-gray-500'>john.doe@example.com</p>
                      </div>
                    </div>
                  </div>
                  
                  <div className='p-2'>
                    <button 
                      onClick={() => { navigate('/profile'); setShowProfile(false); }}
                      className='w-full text-left px-4 py-3 rounded-xl hover:bg-gray-50 transition-colors duration-200 flex items-center gap-3 group'
                    >
                      <div className='w-8 h-8 bg-primary/10 rounded-full flex items-center justify-center group-hover:bg-primary/20 transition-colors duration-200'>
                        <svg className='w-4 h-4 text-primary' fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                        </svg>
                      </div>
                      <div className='flex-1'>
                        <p className='text-sm font-medium text-gray-900'>Profile</p>
                        <p className='text-xs text-gray-500'>Manage your account</p>
                      </div>
                    </button>

                    <button 
                      onClick={() => { navigate('/my-appointments'); setShowProfile(false); }}
                      className='w-full text-left px-4 py-3 rounded-xl hover:bg-gray-50 transition-colors duration-200 flex items-center gap-3 group'
                    >
                      <div className='w-8 h-8 bg-primary/10 rounded-full flex items-center justify-center group-hover:bg-primary/20 transition-colors duration-200'>
                        <svg className='w-4 h-4 text-primary' fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                        </svg>
                      </div>
                      <div className='flex-1'>
                        <p className='text-sm font-medium text-gray-900'>Appointments</p>
                        <p className='text-xs text-gray-500'>View your bookings</p>
                      </div>
                    </button>

                    <div className='border-t border-gray-100 my-2'></div>

                    <button 
                      onClick={() => { setToken(false); setShowProfile(false); }}
                      className='w-full text-left px-4 py-3 rounded-xl hover:bg-red-50 transition-colors duration-200 flex items-center gap-3 group'
                    >
                      <div className='w-8 h-8 bg-red-100 rounded-full flex items-center justify-center group-hover:bg-red-200 transition-colors duration-200'>
                        <svg className='w-4 h-4 text-red-600' fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                        </svg>
                      </div>
                      <div className='flex-1'>
                        <p className='text-sm font-medium text-red-600'>Logout</p>
                        <p className='text-xs text-gray-500'>Sign out of account</p>
                      </div>
                    </button>
                  </div>
                </div>
              </div>
            ) : (
              <div className='hidden sm:flex items-center gap-2'>
                <button 
                  onClick={() => navigate('/login')} 
                  className='px-3 sm:px-4 py-2 text-primary font-medium hover:text-primary/80 transition-colors duration-200 text-sm bg-white rounded-lg border border-primary/20'
                >
                  Sign In
                </button>
                <button 
                  onClick={() => navigate('/login')} 
                  className='px-3 sm:px-4 py-2 bg-gradient-to-r from-primary to-primary/80 text-white rounded-xl font-semibold hover:shadow-lg transform hover:scale-105 transition-all duration-300 text-sm'
                >
                  Sign Up
                </button>
              </div>
            )}

            {/* Mobile Menu Button */}
            <button 
              onClick={handleMenuToggle}
              className='lg:hidden p-2 rounded-xl hover:bg-gray-50 transition-colors duration-200 bg-white shadow-md border border-gray-300'
            >
              <svg className='w-5 h-5 sm:w-6 sm:h-6 text-gray-600' fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />
              </svg>
            </button>
          </div>
        </div>
      </div>

      {/* Mobile Menu */}
      <div className={`lg:hidden fixed inset-0 z-50 ${showMenu ? 'block' : 'hidden'}`}>
        <div className='fixed inset-0 bg-black/70' onClick={() => { setShowMenu(false); setShowProfile(false); }}></div>
        <div className={`fixed right-0 top-0 h-full w-72 sm:w-80 bg-white shadow-2xl transform transition-transform duration-300 ${
          showMenu ? 'translate-x-0' : 'translate-x-full'
        }`}>
          <div className='flex items-center justify-between p-4 sm:p-6 border-b border-gray-100 bg-white'>
            <div className='flex items-center gap-3'>
              <div className='w-10 h-10 bg-gradient-to-r from-primary to-primary/80 rounded-xl flex items-center justify-center'>
                <img src={assets.logo} alt="logo" className="w-6 h-6" />
              </div>
              <h2 className='text-lg font-bold text-gray-900'>DocEase</h2>
            </div>
            <button 
              onClick={() => { setShowMenu(false); setShowProfile(false); }}
              className='p-2 rounded-xl hover:bg-gray-50 transition-colors duration-200 bg-white shadow-sm border border-gray-200'
            >
              <svg className='w-5 h-5 sm:w-6 sm:h-6 text-gray-600' fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
          </div>
          
          <nav className='p-4 sm:p-6 space-y-2 bg-white'>
            <NavLink 
              onClick={() => { setShowMenu(false); setShowProfile(false); }}
              to='/home' 
              className={({ isActive }) => `
                block px-4 py-3 rounded-xl font-medium transition-all duration-200
                ${isActive 
                  ? 'bg-gradient-to-r from-primary to-primary/80 text-white' 
                  : 'text-gray-700 hover:bg-gray-50'
                }
              `}
            >
              Home
            </NavLink>
            
            <NavLink 
              onClick={() => { setShowMenu(false); setShowProfile(false); }}
              to='/doctors' 
              className={({ isActive }) => `
                block px-4 py-3 rounded-xl font-medium transition-all duration-200
                ${isActive 
                  ? 'bg-gradient-to-r from-primary to-primary/80 text-white' 
                  : 'text-gray-700 hover:bg-gray-50'
                }
              `}
            >
              All Doctors
            </NavLink>
            
            <NavLink 
              onClick={() => { setShowMenu(false); setShowProfile(false); }}
              to='/about' 
              className={({ isActive }) => `
                block px-4 py-3 rounded-xl font-medium transition-all duration-200
                ${isActive 
                  ? 'bg-gradient-to-r from-primary to-primary/80 text-white' 
                  : 'text-gray-700 hover:bg-gray-50'
                }
              `}
            >
              About
            </NavLink>
            
            <NavLink 
              onClick={() => { setShowMenu(false); setShowProfile(false); }}
              to='/contact' 
              className={({ isActive }) => `
                block px-4 py-3 rounded-xl font-medium transition-all duration-200
                ${isActive 
                  ? 'bg-gradient-to-r from-primary to-primary/80 text-white' 
                  : 'text-gray-700 hover:bg-gray-50'
                }
              `}
            >
              Contact
            </NavLink>
          </nav>
          
          {token && (
            <div className='p-4 sm:p-6 border-t border-gray-100 space-y-3 bg-white'>
              <button 
                onClick={() => { setShowProfileCard(true); setShowMenu(false); }}
                className='w-full px-4 py-3 bg-gradient-to-r from-pink-500 to-pink-600 text-white rounded-xl font-semibold hover:shadow-lg transition-all duration-300 flex items-center justify-center gap-3'
              >
                <div className='w-6 h-6 bg-white/20 rounded-full flex items-center justify-center'>
                  <svg className='w-4 h-4 text-white' fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                  </svg>
                </div>
                <span>View Profile Card</span>
              </button>
              
              <button 
                onClick={() => { navigate('/my-appointments'); setShowMenu(false); }}
                className='w-full px-4 py-3 text-left rounded-xl hover:bg-gray-50 transition-colors duration-200 flex items-center gap-3'
              >
                <div className='w-8 h-8 bg-primary/10 rounded-full flex items-center justify-center'>
                  <svg className='w-4 h-4 text-primary' fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                  </svg>
                </div>
                <div className='flex-1'>
                  <p className='text-sm font-medium text-gray-900'>My Appointments</p>
                </div>
              </button>

              <button 
                onClick={() => { setToken(false); setShowMenu(false); }}
                className='w-full px-4 py-3 text-left rounded-xl hover:bg-red-50 transition-colors duration-200 flex items-center gap-3'
              >
                <div className='w-8 h-8 bg-red-100 rounded-full flex items-center justify-center'>
                  <svg className='w-4 h-4 text-red-600' fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                  </svg>
                </div>
                <div className='flex-1'>
                  <p className='text-sm font-medium text-red-600'>Logout</p>
                </div>
              </button>
            </div>
          ) : (
            <div className='p-4 sm:p-6 border-t border-gray-100 space-y-3 bg-white'>
              <button 
                onClick={() => { navigate('/login'); setShowMenu(false); }}
                className='w-full px-4 py-2 text-primary font-medium hover:bg-primary/10 rounded-xl transition-colors duration-200 bg-white border border-primary/20'
              >
                Sign In
              </button>
              <button 
                onClick={() => { navigate('/login'); setShowMenu(false); }}
                className='w-full px-4 py-3 bg-gradient-to-r from-primary to-primary/80 text-white rounded-xl font-semibold hover:shadow-lg transition-all duration-300'
              >
                Sign Up
              </button>
            </div>
          )}
        </div>
      </div>

      <style jsx>{`
        @keyframes fade-in {
          from { opacity: 0; transform: translateY(10px); }
          to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-in { animation: fade-in 0.3s ease-out; }
      `}</style>

      {/* Profile Card Modal */}
      <ProfileCardModal 
        isOpen={showProfileCard} 
        onClose={() => setShowProfileCard(false)} 
        userData={userData}
      />
    </nav>
  )
}

export default Navbar